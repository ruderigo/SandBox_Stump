# Project Stump -- stumpid
# Optional Ed25519 identity verification for RRC. See plugin.json for
# the wizard-facing description and core.py / install.py for the
# implementation. Not imported directly by anything else; example_node.py
# reaches it only through install.activate(), the same way every other
# plugin is loaded.
